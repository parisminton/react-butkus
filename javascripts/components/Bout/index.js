'use strict'

const React = require('react')
const ReactDOM = require('react-dom')
const PropTypes = require('prop-types')
const { Component } = React

class Bout extends Component {
  render() {
    return <div>MY BOUT</div>
  }
}

module.exports = Bout
